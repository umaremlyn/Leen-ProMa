databases:
  - name: pms
    databaseName: leenproma
    user: proma
    password: RsCRrHUYALLEjC3Qlg6KDuDob6OeVtpA
      fromDatabase:
        name: pms
        property: credentials.password

services:
  - type: web
    name: proma
    envVars:
      - key: DATABASE_URL
        value: "postgres://myuser:${PASSWORD}@${HOST}:${PORT}/mydatabase"
      - key: SECRET_KEY
        generateValue: true
    buildCommand: "./build.sh"
    startCommand: "gunicorn mysite.wsgi:application"